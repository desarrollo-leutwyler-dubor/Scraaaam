version: '2'
services:
  mongo:
    image: mongo
    ports:
      - "27018:27017"
    command: "--smallfiles --logpath=/dev/null"

  app:
    build: .
    depends_on:
      - mongo
    ports:
      - "3001:3001"
    links:
      - mongo
    environment:
      MONGO_URL: 'mongodb://mongo/projects'